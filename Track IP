HTML print $ apt update && apt upgrad
$ git clone
$ git clone https://github.com/htr-tech/track-ip
cd track-ip
chmod +x trackip
bash trackip
And type 👽
Run : bash trackip

Single Command :👽👽
apt update ; apt install git curl -y ; git clone git://github.com/htr-tech/track-ip.git ; cd track-ip ; bash trackip
